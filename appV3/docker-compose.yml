version: '3.8'

services:
  # Flask Web Application
  jobsite-web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: jobsite-flask
    ports:
      - "5000:5000"
    environment:
      - FLASK_APP=run.py
      - FLASK_ENV=development
      - FLASK_DEBUG=1
      - SECRET_KEY=${SECRET_KEY:-dev-secret-key-change-in-production}
      - DATABASE_URL=mssql+pyodbc://sa:YourStrong%40Passw0rd@sqlserver:1433/JobsDB?driver=ODBC+Driver+18+for+SQL+Server&TrustServerCertificate=yes
    depends_on:
      sqlserver:
        condition: service_healthy
    networks:
      - jobsite-network
    volumes:
      - .:/app
    command: flask run --host=0.0.0.0 --debug
    restart: unless-stopped

  # SQL Server Database
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: jobsite-sqlserver
    environment:
      ACCEPT_EULA: "Y"
      MSSQL_SA_PASSWORD: "YourStrong@Passw0rd"
      MSSQL_PID: "Developer"
    ports:
      - "1433:1433"
    volumes:
      - sqlserver-data:/var/opt/mssql
    networks:
      - jobsite-network
    healthcheck:
      test: /opt/mssql-tools18/bin/sqlcmd -S localhost -U sa -P "YourStrong@Passw0rd" -C -Q "SELECT 1" || exit 1
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s
    restart: unless-stopped

  # Azure Data Studio Web (optional - for database management)
  # You can also connect using Azure Data Studio or SSMS from your host

networks:
  jobsite-network:
    driver: bridge

volumes:
  sqlserver-data:
